#ifndef ENUMS_H
#define ENUMS_H

enum Id {
	none,
	fire,
	earth,
	water,
	wind
};

enum PType {
	score
};

enum Dir {
	emtpy,
	up,
	down,
	left,
	right
};

enum SFX {
	playerJump1 = 0,
};



#endif // !ENUMS_H
